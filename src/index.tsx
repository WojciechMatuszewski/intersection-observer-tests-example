import React from "react";
import ReactDOM from "react-dom";
import "./index.css";

ReactDOM.render(
  <React.StrictMode>
    <div>
      Testing <code>IntersectionObserver</code> API
    </div>
  </React.StrictMode>,
  document.getElementById("root")
);
